{
  "functions": {
    "api/generate-story.js": {
      "maxDuration": 300,
      "memory": 1024
    },
    "api/upload-image.js": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "buildCommand": "npx vite build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
